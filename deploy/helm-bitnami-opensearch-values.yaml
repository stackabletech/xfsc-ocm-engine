---
global:
  kibanaEnabled: false

volumePermissions:
  enabled: false
  securityContext:
    runAsUser: auto

master:
  heapSize: "256m"
  podSecurityContext:
    enabled: true
  containerSecurityContext:
    enabled: true
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "1Gi"
      cpu: "400m"

data:
  heapSize: "1024m"
  persistence:
    enabled: true
    size: "8Gi"
  podSecurityContext:
    enabled: true
  containerSecurityContext:
    enabled: true
  resources:
    requests:
      memory: "512Mi"
      cpu: "100m"
    limits:
      memory: "1664Mi"
      cpu: "400m"

security:
  tls:
    restEncryption: true
    autoGenerated: true

